@page
@model KE03_INTDEV_SE_1_Base.Pages.LoginModel
@{
    ViewData["Title"] = "Inloggen";
}
<h1>@ViewData["Title"]</h1>

<p>Selecteer een gebruiker om in te loggen.</p>

<form method="post">
    <div>
        @foreach (var c in Model.Customers)
        {
            <div>
                <input type="radio" class="matrix-radio" asp-for="SelectedCustomerId" name="SelectedCustomerId" value="@c.Id" id="user-@c.Id" />
                <label for="user-@c.Id">@c.Name (@c.Address)</label>
            </div>
        }
    </div>

    <div class="matrix-button-row">
        <button type="submit" class="matrix-button">Login</button>
        <a href="/Register" class="matrix-button matrix-button-link">Account Aanmaken</a>
    </div>
</form>

@if (!string.IsNullOrEmpty(Model.ErrorMessage))
{
    <p style="color:red">@Model.ErrorMessage</p>
}